#include "json_parser.h"

// JsonParser is primarily a header-only utility class with template functions.
// This file provides a compilation unit for any non-template implementations
// and ensures the header is valid.

namespace FEA {

// Explicit template instantiations if needed for specific dimensions
// Currently all functions are template and inline in the header.

} // namespace FEA
